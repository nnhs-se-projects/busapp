<!DOCTYPE html>
<html>
    <head>
        <link rel="icon" type="image/x-icon" href="/img/busapp-favicon.png">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            * {margin: 0;}
            html {
                font-family: sans-serif;
                color: white;
                text-align: center;
            }
            .backdrop {
                position: fixed;
                min-width: calc(100% + 40px);
                min-height: calc(100% + 40px);
                width: calc(100% + 40px);
                height: calc(100% + 40px);
                object-fit: cover;
                left: calc(-50% - 40px);
                top: calc(-50% - 40px);
                transform: translateX(50%) translateY(50%);
                filter: blur(5px) brightness(65%);
            }
            .container {
                box-sizing: border-box;
                height: 100%;
                width: 100%;
                position: fixed;
                padding: 30px;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: min(4vw, 5vh);
            }
            h1 {
                font-size: min(40vw, 60vh);
            }
        </style>
    </head>
    <body>
        <img class="backdrop" src="/img/404_background.webp"></div>
        <div class="container">
            <div>
                <h1>404</h1>
                <h2>"<%= url %>" Does not exist</h2>
                <a href="/"><h3>Go Home</h3></a>
            </div>
        </div>
    </body>
</html>